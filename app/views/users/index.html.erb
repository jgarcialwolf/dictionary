<div class="container mt-5">
  <% @users.each do |user| %>
    <% if user.username == current_user.username %>
      <div><%= link_to "My public profile", user %></div>
      <% next %>
    <% end %>
    <div><%= link_to user.username, user %></div>
  <% end %>
</div>
